{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "bin": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node .",
    "build": "cd ../client && yarn run build && mv build ../server/assets",
    "dev": "concurrently \"nodemon server.js\" \"cd ../client && yarn start\"",
    "export:exe": "pkg . --target node12-win-x64 --output server.exe"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "concurrently": "^8.0.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pkg": "^5.8.1",
    "sqlite3": "^5.1.6"
  }
}
